# 分镜功能可用性优化计划

> 针对用户反馈的分镜编辑体验问题，制定系统性改进方案。

## 一、背景与问题分析

当前分镜功能在实际使用中暴露以下核心问题：

### 问题 1：冗余的"画面内容概述"字段
- **现象**：`summary` 字段与 `scene` + `action` 信息重复，用户需额外填写无实际意义的概述，增加认知负担
- **影响**：编辑效率低，字段职责不清晰
- **根因**：早期设计未明确区分"概述"与"动作"的边界，导致功能重叠

### 问题 2：景别与镜头运动选项不足
- **现象**：当前仅支持 5 种景别、7 种镜头运动，无法覆盖专业分镜需求
- **影响**：用户无法准确描述镜头语言，需手动输入非标准选项
- **根因**：选项列表基于简化模型，未参考行业标准镜头分类

### 问题 3：AI 拆分粒度过粗且缺乏专业指导
- **现象**：
  - 单次最多输出 8 个镜头，导致长剧本被过度压缩
  - 8000 字符分块过大，AI 倾向于生成概括性镜头而非画面级拆分
  - 长剧本无法在一次会话中完整拆分，需人工补充分镜
  - 对白与动作绑定不准确，出现独立台词镜头或动作错位
  - 括号或 HTML 注释中的画面匹配指令（如"画面匹配上面台词的后半句"）未被识别
- **影响**：拆分结果可用性差，需大量人工调整，专业用户满意度低
- **根因**：分块策略与 Prompt 设计偏向保守，未针对画面变化点优化，缺乏专业分镜规则指导

当前 `storyboard-analysis-service.ts` 以 8000 字符按空行切块，超长段落会硬切；系统 Prompt 和用户 Prompt 均要求最多 8 镜。即使 JSON 可解析，`storyboard-response-parser.ts` 仍可能跳过无效项，`storyboard-validator.ts` 也未验证剧本覆盖（空数组可通过）。截断后递归二分仅处理格式失败，无法发现“合法 JSON 但省略后半段”的情况。当前服务先合并全部结果再替换正式文档；第 3 阶段必须保留这一安全边界。

---

## 二、优化目标

| 目标 | 衡量指标 |
|------|----------|
| 减少无效输入字段 | 编辑界面字段数减少 30% |
| 覆盖专业镜头语言 | 景别 ≥9 种，镜头运动 ≥15 种 |
| 提升 AI 拆分精度 | 以视觉节拍和原文覆盖衡量，不设每千字镜头配额 |
| 支持长剧本完整拆分 | 单次任务可分批处理 ≥50000 字剧本；仅在全部可拍摄单元覆盖后标记完成 |

---

## 三、详细实施方案

### 阶段 1：移除冗余字段（优先级：高）

#### 1.1 UI 层调整
- **编辑面板** (`ShotContentEditor.tsx`)
  - 删除"画面内容概述"编辑区块
  - 保留 `scene`、`action`、`dialogue` 三个核心字段
  - 调整字段顺序：场景 → 动作 → 对白

- **表格显示** (`StoryboardRow.tsx`)
  - "画面内容描述"列改为直接拼接 `scene` + `action` 显示
  - 移除对 `summary` 的依赖

#### 1.2 数据层兼容
- **类型定义** (`src/types/storyboard.ts`)
  - 保留 `summary` 字段以确保向后兼容
  - 添加注释标记该字段为"已废弃，仅用于旧数据兼容"

- **数据迁移**
  - 项目打开时自动检测：若 `action` 为空且 `summary` 非空，将 `summary` 内容填充至 `action`
  - 在 `storyboard-file-service.ts` 中添加迁移逻辑

#### 1.3 AI 输出格式调整
- **Prompt 更新** (`storyboard-analysis-service.ts`)
  - 从输出 JSON 结构中移除 `summary` 字段要求
  - 明确指示 AI 只需输出 `scene`、`action`、`dialogue`

---

### 阶段 2：扩充镜头语言选项（优先级：高）

#### 2.1 标准化选项定义
创建集中管理的镜头语言常量文件：

```typescript
// src/lib/storyboard/shot-options.ts

/** 景别选项（按取景范围从小到大） */
export const SHOT_SIZES = [
  { value: "极特写", label: "极特写 (ECU)", description: "眼睛、嘴唇等局部细节" },
  { value: "特写", label: "特写 (CU)", description: "面部、手部等关键部位" },
  { value: "近景", label: "近景 (MCU)", description: "胸部以上" },
  { value: "中近景", label: "中近景 (MS)", description: "腰部以上" },
  { value: "中景", label: "中景 (MFS)", description: "膝盖以上" },
  { value: "中远景", label: "中远景 (MLS)", description: "全身" },
  { value: "全景", label: "全景 (FS)", description: "全身+环境" },
  { value: "远景", label: "远景 (LS)", description: "人物占画面 1/4" },
  { value: "大远景", label: "大远景 (ELS)", description: "环境为主，人物渺小" },
] as const;

/** 镜头运动选项 */
export const CAMERA_MOVEMENTS = [
  { value: "固定", label: "固定 (Static)", description: "机位不动" },
  { value: "推", label: "推 (Dolly In)", description: "向主体靠近" },
  { value: "拉", label: "拉 (Dolly Out)", description: "远离主体" },
  { value: "摇", label: "摇 (Pan)", description: "水平转动" },
  { value: "俯仰", label: "俯仰 (Tilt)", description: "垂直转动" },
  { value: "移", label: "移 (Trucking)", description: "水平移动" },
  { value: "跟", label: "跟 (Follow)", description: "跟随主体移动" },
  { value: "升降", label: "升降 (Crane)", description: "垂直升降" },
  { value: "环绕", label: "环绕 (Arc)", description: "围绕主体旋转" },
  { value: "手持", label: "手持 (Handheld)", description: "手持晃动效果" },
  { value: "斯坦尼康", label: "斯坦尼康 (Steadicam)", description: "稳定器跟拍" },
  { value: "无人机", label: "无人机 (Aerial)", description: "航拍视角" },
  { value: "变焦", label: "变焦 (Zoom)", description: "镜头焦距变化" },
  { value: "甩", label: "甩 (Whip Pan)", description: "快速甩镜转场" },
  { value: "主观", label: "主观 (POV)", description: "角色视角" },
] as const;
```

#### 2.2 UI 层应用
- **编辑面板** (`ShotContentEditor.tsx`)
  - 替换现有选项数组为新常量
  - 为复杂选项添加 `title` 提示（悬停显示详细说明）

- **AI Prompt** (`storyboard-analysis-service.ts`)
  - 更新 Prompt 中的选项提示，列出完整可选值
  - 指示 AI 根据剧本内容选择最合适的专业术语

---

### 阶段 3：AI 拆分粒度与精度优化（优先级：高）

> 参考 `skill/storyboard_SKILL.md` 中的专业拆分指南，实现画面级精细拆分。

#### 3.1 核心拆分原则（基于原剧本与人工样本校准）

> 人工分镜表只用于校准“画面内容如何形成镜头”和“对白应落在哪个画面”。不参考其景别、机位、运镜，也不直接复制其中改写、增删的剧情或对白。剧本原文始终是内容完整性、对白原文和先后顺序的唯一事实来源。

##### 3.1.1 镜头的基本单位：单一视觉节拍

一个镜头对应一个在同一时空中连续发生、可以被独立描述和拍摄的**视觉节拍**。判断重点不是句号、逗号或对白条数，而是观众正在看的主体、主体状态和画面任务是否保持连续。

每个镜头必须至少有一项可见内容：
- 人物动作或表情反应
- 环境、人物或关键道具的建立与揭示
- 魔法、特效、字幕或转场的发生过程
- 动作造成的可见结果

对白、画外音和音效不能单独构成空画面，必须绑定到其发生时观众实际看到的视觉节拍。

##### 3.1.2 先拆视觉事件链，再绑定对白

先把动作段落拆成以下事件节点，再决定相邻节点能否合并：

1. **建立**：交代新场景、人物位置、威胁或关键物体
2. **发起**：人物作出决定、施法、攻击、移动或操作
3. **显现**：新的对象、特效或信息进入画面
4. **作用**：攻击、魔法或动作命中目标并产生过程
5. **结果**：环境或人物状态发生明确改变
6. **反应**：人物观察、震惊、犹豫、疼痛或情绪转折

同一事件链不要求每个节点都单独成镜，但出现以下情况时必须拆分：
- 场次、地点、时间或现实层级变化，例如村郊进入意识海
- 主要可见主体改变，例如红袍人影转为地下石门和银色守卫
- 主体状态发生不可逆变化，例如站立变为倒地、存活变为死亡
- 新威胁、新角色、关键道具、字幕、转场或大规模特效首次显现
- 动作目的从“发起”进入“命中/作用”，或从“作用”进入“结果/反应”
- 原文明确要求画面对应某句或某一段对白
- 一个动作段中存在可独立审阅、不可由同一画面同时清楚表达的连续阶段

人工样本体现的典型拆法：
- “红雾遮蔽 → 迷雾散去并显现石门守卫 → 守卫抬剑挥来”按遮蔽、揭示、攻击三个视觉阶段处理，不把整段压成概述镜头
- “咬人甘蓝出现 → 冲向敌人并淹没前排 → 火浪点燃甘蓝 → 手提箱与武士刀小狗出现 → 拔刀斩 → 碎块落下并收刀”按新元素、交锋、反制和结果拆分
- “蛇怪扑出 → 莱拉推开洛蓝 → 对上蛇怪眼睛 → 声音消失 → 莱拉倒下 → 洛蓝确认死亡并反应”按危险出现、救人、致死触发、结果和情绪反应拆分

##### 3.1.3 对白按画面语义归属，不按说话人机械切镜

- 对白绑定到说话发生时或原文指定的画面，不默认绑定“最近的前一个动作”
- 同一连续双人画面内的问答可以保留在同一镜头；只有画面主体、人物反应或叙事重点改变时才切镜
- 台词跨越多个视觉节拍时，按句意或原文标记分段绑定；各镜头可保存对应片段，但不得改写、漏写或重复整句
- “画面匹配洛蓝后半句台词”表示洛蓝对白的后半句继续播放，但画面已切到莱拉的担忧或释然反应
- “后半句台词匹配洛蓝语塞”表示黑巫师 B 的对白继续播放，画面主体切到洛蓝无言以对的反应
- 画外音绑定到同期展示的动作或环境；若原文没有明确画面，则挂到最符合上下文的视觉节拍并在备注标记推断
- “台词交由 AI 自由发挥”等原文指令必须原样保留，不能自行生成正式对白替换

##### 3.1.4 相邻内容的合并条件

只有同时满足以下条件时，相邻动作或对白才可合并为一个镜头：
- 同一场次、同一连续时间
- 主要可见主体一致
- 动作目的与画面任务一致
- 中间没有新的信息揭示、结果落点或人物反应
- 合并后仍能用一个清晰画面描述，而不是用“随后、紧接着、片刻后”串联多个独立阶段

短动作不为增加镜头数而强拆；长复合动作也不为满足固定输出数量而合并。镜头数量由视觉节拍覆盖决定。

##### 3.1.5 拆分执行顺序

1. 按场次标题建立硬边界，确保 `scene` 不跨场次
2. 按场次、动作、角色对白、括号提示、转场/字幕、HTML 注释六类语法提取有序原文单元，并保存来源位置
3. 将动作段解析为建立、发起、显现、作用、结果、反应等视觉事件
4. 根据必须拆分条件形成候选镜头，再按合并条件消除过度切分
5. 将每句对白或对白片段绑定到对应画面，处理画外音和括号/HTML 注释中的显式画面指令
6. 按源单元 ID 与原文偏移逐段核对覆盖率，确认没有遗漏动作、对白、字幕、转场、特效和人物反应

**禁止行为**：
- 参考人工表的景别、机位或运镜决定是否切镜
- 按标点、句长、说话人变化或固定数量机械切分
- 为达到“每块若干镜头”的配额合并或扩写画面
- 生成只有角色名、对白或冒号而没有可见内容的镜头
- 使用人工表中与原剧本不一致的改写对白或新增剧情覆盖原文

#### 3.2 剧本格式解析规则

输入剧本采用以下六类结构化标记。解析器应先进行确定性语法解析，再将结构化结果交给 AI 拆分，避免由模型猜测场次、说话人或注释用途。

```markdown
## 场次：时间 / 内外景 / 地点
△ 描述人物动作、环境和画面变化
**角色名**：
对白内容
（语气或动作）
【转场：切至下一场】
【字幕：画面字幕内容】
<!-- 创作注释 -->
```

```typescript
type ScriptToken =
  | { type: 'scene'; time: string; interiorExterior: string; location: string }
  | { type: 'action'; content: string }
  | { type: 'dialogue'; character: string; cue?: string; content: string }
  | { type: 'parenthetical'; content: string; owner: 'dialogue' | 'action' }
  | { type: 'transition'; content: string }
  | { type: 'subtitle'; content: string }
  | { type: 'comment'; content: string }
  | { type: 'unknown'; content: string; reason: string };

interface ParsedToken<T extends ScriptToken = ScriptToken> {
  token: T;
  id: string; // 同一剧本版本内稳定；由源范围、类型与内容确定
  rawText: string;
  sourceRange: { start: number; end: number };
  lineRange: { start: number; end: number };
}
```

##### 3.2.1 场次

- 标准格式：`## 场次：时间 / 内外景 / 地点`
- 解析为 `time`、`interiorExterior`、`location`，三部分去除首尾空格但保留原文
- 场次标记是硬边界；之后的镜头只能使用当前场次信息，直到遇到下一场
- `content.scene` 以地点为主体，时间与内外景作为结构化上下文保留
- 字段缺失或斜杠数量异常时，不猜测缺失值，将整行写入 `notes` 并标记 `[待确认：场次格式]`

##### 3.2.2 动作

- 标准格式：行首 `△`，其后内容是观众可看到或听到的动作、环境和画面变化
- 一个 `△` 动作块可以包含多个视觉节拍，仍需按 3.1 的事件链继续拆分，不能将一个动作块固定视为一个镜头
- 动作原文是 `action` 和 `sourceText` 的主要来源；可听见的音效保留在 `additionalDescription`
- 连续的非空普通文本若属于同一 `△` 段，作为该动作块的续行处理

##### 3.2.3 角色与对白

- 标准格式：角色提示单独成行，写作 `**角色名**：`，对白位于其后的非空行
- 兼容现有剧本中的 `角色名（语气或动作）：` 和 `角色名：对白内容`，但输出时统一保留角色名、提示和对白原文
- 角色提示不是独立画面；角色名写入对白归属，括号内语气、动作、语言和画外音信息写入 `cue`
- 对白持续读取到空行或下一条明确语法标记，不把对白中的冒号误判为新角色
- 对白块随后按 3.3 绑定到实际视觉节拍，不因角色行出现就自动新建镜头

##### 3.2.4 括号提示

- `（语气或动作）`、`(语气或动作)` 及角色名后的括号都属于表演或声音提示
- 位于角色提示或对白内部时归属 `dialogue`，用于语气、语言、画外音、停顿或同期动作
- 位于动作块内部时归属 `action`，用于补充人物状态或画面执行要求
- 括号中的可见动作参与视觉节拍判断；纯语气、语言或声音属性不单独成镜
- 无法判断归属时保留到 `notes` 并标记 `[待确认：括号归属]`

##### 3.2.5 转场与字幕

- `【转场：内容】` 解析为 `transition`；其内容描述实际转场画面时，应形成或附着到可拍摄镜头
- `【字幕：内容】` 解析为 `subtitle`；字幕必须绑定到显示该文字的实际画面，并进入 `dialogue` 或字幕专用数据
- 转场和字幕均保留完整 `sourceText`，不得只保存冒号后的内容而丢失标记类型
- 未支持的 `【类型：内容】` 不擅自解释，整段进入 `notes` 并标记 `[待确认：未知方括号标记]`

##### 3.2.6 HTML 创作注释

- `<!-- 创作注释 -->` 是不会直接出现在成片中的创作提示，不作为角色对白或可见动作输出
- 注释可用于约束拆分、氛围、节奏、参考风格、镜头范围或对白画面匹配，并写入相关镜头的 `notes` 或 `additionalDescription`
- 注释明确限定画面时必须遵守，例如“画面中仅出现洛蓝手部动作”会限制该镜头可见主体
- 注释包含画面匹配指令时，按 3.3 绑定对应对白片段；无法可靠解析时原样保留并标记 `[待确认：创作注释]`
- 注释本身不能生成独立镜头，也不能被改写成成片中可见或可听的内容

**通用处理策略**：
1. 所有 token 保留 `rawText`、字符范围和行范围，保证生成结果可追溯
2. 语法识别优先于 AI 推断；明确标记不得被模型改换类型
3. 格式冲突时保留原文顺序，不静默丢弃内容，并在 `notes` 中记录冲突
4. 不符合六类语法的文本作为 `unknown` 保留，标记 `[待确认：未识别格式]`
5. 偏移均针对读入时的原始剧本文本；标准化换行只用于识别，不得改变用于回填 `sourceTextRange` 的原文索引；场次上下文可跨批传递，但场次自身不形成镜头

#### 3.3 对白-动作绑定机制

确保每句对话和动作都在正确的分镜上：

```typescript
interface DialogueActionBinding {
  /** 原文对白单元 ID 与绑定的视觉事件 ID */
  dialogueUnitId: string;
  visualEventId: string;
  /** 对话内容 */
  dialogue: string;
  /** 说话角色 */
  character?: string;
  /** 对应的动作描述 */
  action: string;
  /** 源文本范围（原文偏移） */
  sourceRange: { start: number; end: number };
  /** 对白在原台词中的片段范围，整句绑定时可省略 */
  dialogueRange?: { start: number; end: number };
  /** 对白应匹配的视觉事件或画面主体 */
  visualTarget: string;
  /** 绑定依据 */
  bindingReason: 'same_block' | 'speaker_action' | 'reaction' | 'explicit_marker' | 'context_inference';
  /** 是否为画外音 */
  isVoiceover: boolean;
  /** 是否为内心独白 */
  isMonologue: boolean;
}

/**
 * 绑定规则：
 * 1. 优先服从括号或 HTML 注释中的显式画面匹配指令
 * 2. 无显式标记时，按同一剧本块、说话动作和上下文语义确定画面
 * 3. 对白跨越反应画面时按语义边界保存对应片段，不使用固定句数切分
 * 4. 同一连续画面中的问答可合并，不因说话人变化自动切镜
 * 5. 画外音标记 isVoiceover=true，但仍须指定实际 visualTarget
 * 6. 无法确定归属时使用 context_inference，并在 notes 标记待确认
 */
```

#### 3.4 Skill 辅助拆分功能

集成 `skill/storyboard_SKILL.md` 中的质量检查规则：

```typescript
// src/lib/storyboard/skill-validator.ts

interface SkillValidationResult {
  valid: boolean;
  issues: SkillIssue[];
  /** 必需源单元是否全部有经校验的镜头归属 */
  sourceCoverageComplete: boolean;
  /** 对白-动作绑定是否正确 */
  dialogueBindingCorrect: boolean;
  /** 六类剧本语法是否被正确解析并覆盖 */
  scriptSyntaxHandled: boolean;
}

interface SkillIssue {
  type: 'empty_action' | 'dialogue_only' | 'missing_source' | 'orphan_dialogue' | 'syntax_unparsed' | 'comment_rendered' | 'uncovered_unit' | 'duplicate_dialogue' | 'source_mismatch';
  shotIndex: number;
  message: string;
  suggestion: string;
}

/**
 * 质量检查清单：
 * - 对非空可拍摄输入，shots 非空，order 从 0 连续递增
 * - 每个镜头的 action、scene、sourceText 非空
 * - 没有只有角色名或只有台词的镜头
 * - 所有场次边界均被遵守，关键转场、字幕、魔法、战斗、道具和人物反应均被覆盖
 * - 长复合句已按画面阶段拆分
 * - 对白仍附着在有动作的镜头上
 * - 场次、动作、角色对白、括号提示、转场/字幕和 HTML 注释均被正确分类
 * - HTML 注释未被错误输出为成片画面或对白
 * - 未识别格式被完整保留并记录在 notes 中；阻断性归属歧义标记待确认，不伪称完整
 */
export function validateWithSkillRules(shots: StoryboardShot[]): SkillValidationResult
```

#### 3.5 分层调用与预算感知分批

1. **预处理（本地、无模型调用）**：按 3.2 解析六类语法，保留原始偏移与场次；将复合 `△` 动作提取为候选视觉事件（建立/发起/显现/作用/结果/反应）。语义不确定的事件边界可交由模型细分，但每个候选事件仍保留所覆盖的源 token ID 与范围；建立对白片段及其显式匹配约束。场次不生成镜头，注释不生成成片内容。
2. **事件组分批**：以单场内连续、完整的视觉事件组为最小调度单位；角色提示+对白、括号和其归属、转场/字幕与目标画面、跨画面对白及注释约束作为不可断开的绑定组。批次可包含少量邻接组，顺序连续，不跨场；只读前后文与当前场次作为上下文提供，但显式标记“上下文非生成范围”，避免重复镜头。超大动作 token 先提取内部可追溯的视觉子事件，绝不按字符硬切；无法在预算内安全拆分时停止并提示人工确认或更换模型。
3. **模型预算**：每次调用根据配置模型的可用上下文窗口与输出上限预留系统提示、结构化事件、只读上下文及 JSON 输出空间；以事件复杂度预估镜头数和每镜 token 成本，并留安全余量。3000 字符仅可作为初始观测阈值，不能当作所有模型的保证值；预算未知时采用保守小批，以可取得的实际 token 用量或输出长度/截断信号自适应缩小。调用仍通过 `callFeatureAPI('script_analysis', ...)`，传入预算允许的 `maxTokens` 与可选 `modelOverride`；模型配置不支持该预算则报错而非静默压缩内容。
4. **小批生成**：只要求覆盖本批源事件，不设“最多 8 镜”或“约 8 镜”等固定数量；批次过大时拆成更小完整组而不是强迫输出更少镜头。超长结果可按未完成的事件 ID 继续请求下一页，明确已完成/待处理 ID 和页边界；已校验的页不重复生成，不以模型自称“完成”作为覆盖证明。复杂组的输出依然超限时，在合法事件子边界缩小或让用户选更大模型，绝不接受截断 JSON 的前半部分作为完整结果。

建议服务层契约（生成中间态，不直接改现有 `StoryboardShot` 持久化结构）：

```typescript
interface SourceUnit {
  id: string;
  kind: 'visual-event' | 'dialogue' | 'transition' | 'subtitle' | 'comment' | 'unknown';
  sourceRange: { start: number; end: number };
  sceneId: string;
  required: boolean; // comment 为约束；场次标题仅作边界
}
interface ShotDraft {
  content: StoryboardShotContent;
  sourceUnitIds: string[]; // 本批范围内的视觉事件/转场/字幕
  dialogueSlices: Array<{ unitId: string; start: number; end: number }>;
  sourceRanges: Array<{ start: number; end: number }>;
  references?: { characters: string[]; costumes: string[]; scenes: string[] };
  notes?: string;
}
```

`sourceRanges` 支持不连续原文。发布时由服务层从原始剧本范围回填 `sourceText` 和现有 `sourceTextRange`（仅当连续时），不依赖模型自由摘抄；非连续归属的完整映射保存在版本化的来源关联元数据中，避免单个范围伪称连续。落实此契约时同步评估 `storyboard-file-service.ts` 的读写兼容和迁移。

#### 3.6 Prompt 与模型响应契约

- 系统提示保留 3.1-3.3 的画面节拍、六类语法和对白语义规则，移除 `buildSystemPrompt` 中“最多 8 个镜头”和 `buildUserPrompt(..., 8)` 的暗示；同时审查 `script-parser.ts` 中的 6-8 镜 Prompt，仅在确认为此调用路径的一部分时修改，避免误伤其他功能。
- 用户提示提供带 ID、原文范围、场次及约束的本批事件，以及仅供理解的邻接上下文；明确“仅输出本批，按源顺序覆盖每个必需单元，可为同一 `△` 生成多个镜头”。不让模型复制人工样本中的机位/运镜或改写原剧本。
- 模型返回结构化 JSON：每镜提供 `sourceUnitIds`、对白单元 ID 与原文内片段范围、`content`、参考项；批次末尾返回尚未完成的单元 ID（分页游标）。如模型不支持结构化输出，可解析 JSON 但必须严格验证所有条目，不能忽略非法镜头。`sourceText` 和正式镜头编号由本地赋值。
- 字幕和转场须对应实际画面；注释只参与约束/备注，未知格式原样记录 `[待确认]`。对白原文从源范围回填，跨画面片段按范围拼接与原文比对，角色提示及语气保留；“自由发挥”仅保留为待处理指令，不凭空生成台词。

#### 3.7 覆盖审计与定向修复

在 `storyboard-response-parser.ts` 和 `storyboard-validator.ts` 的格式校验后增加独立源覆盖审计，输入为原始单元、批次边界、镜头草稿和对白映射：

1. 对非空可拍摄输入拒绝 `[]`；拒绝被解析器跳过的对象、未知 ID、跨场 ID、范围越界、逆序和未标记的重复引用。每个必需视觉事件/字幕/转场有可见镜头归属；动作 token 中提取的视觉子事件逐一核对。注释和场次分别校验“约束已应用/已记录”与“没有跨场”，不以注释本身生成镜头。
2. 对每个对白单元按原文偏移核对片段的并集与顺序，保证不漏写、不改写、无非预期重叠或重复；显式“前半句/后半句”须指向正确画面事件。对未能确定语义归属的文本和约束输出待确认诊断；阻断性遗漏不得标记成功。
3. 对缺失/冲突的源 ID 发起小范围定向修复，携带原文、约束、相邻已验收镜头的只读摘要，禁止重写已确认批次；每个 ID 有重试上限。修复后重新审计全批及相邻边界；仍不通过时保留草稿与诊断，正式文档不变。格式合法、模型声称完成或镜头数接近人工样本都不是通过依据。
4. 所有批次通过后按原文顺序合并、去除真正重复的边界镜头并再次核对源 ID/对白范围，重新编号；原有角色/场景引用匹配、禁止字段校验继续执行。覆盖率以必需源单元和对白原文范围为分母呈现，不能只报告已生成镜头数。

#### 3.8 草稿预览、取消与断点续传

- 服务将每批/每页**已审计通过**的结果及未通过诊断写入隔离的任务草稿或版本化检查点，不写 `document.shots`。UI 在独立预览中显示已覆盖单元、待处理单元、草稿镜头数及待确认项；进度按已验收源单元而非批次数或字符数计算，100% 仅表示全局审计且正式提交成功。
- 自动顺序推进，允许暂停/继续/取消。先打通 `AbortController` 的 `signal` 从 `callFeatureAPI` 到非流式 `callChatAPI`/底层请求的传递（当前接口声明了 `signal` 但实现未转发），在返回后仍检查任务身份；取消或失败时现有文档、选中镜头、dirty 状态和版本不变。仅在全局校验成功且文档 ID/剧本哈希仍匹配时，将旧版本快照与正式镜头替换作为同一提交更新并进入 review；多任务并发只允许当前任务提交。
- 检查点保存项目/文档 ID、源路径与内容哈希、解析规则/Prompt/模型版本、源单元 ID 清单、已验收批次与映射、未处理 ID、诊断和时间戳；写入须原子化并限量，不将任务草稿塞入 `StoryboardDocument.analysisProgress` 或持久化完整快照。恢复前重新读取源剧本并核对哈希与规则版本；不匹配时要求重新分析，绝不以单个 `processedOffset` 跳过未审计的文本。
- 明确操作入口：继续同一草稿、放弃草稿、重新开始；失败后的草稿只供预览/修复，不可作为“完整分镜”保存或发送到蓝图。针对旧 `analysisJob` 持久化状态处理应用重启后的 running 状态，重启后提供可恢复检查点而非误报任务仍在运行。

#### 3.9 实施文件与测试

- `storyboard-analysis-service.ts`：将空行硬切、8 镜 Prompt、单次大数组和截断后二分替换为解析→预算分批→生成→审计→定向修复→全局发布；使用实际取消信号和任务身份校验。
- 新建/扩展剧本语法解析、视觉事件组、源映射与覆盖审计模块；`storyboard-response-parser.ts` 不得静默忽略无效条目，`storyboard-validator.ts` 校验必需 ID、对白片段和空输入边界；更新 `src/types/storyboard.ts` 及 `storyboard-file-service.ts` 的来源关联兼容。
- `useStoryboardAnalysis.ts`、`AnalysisProgress.tsx` 和 store：展示隔离预览、校验进度、待确认、续传/放弃；完成前旧文档保持可用，发布后再允许编辑新镜头。
- 更新 `storyboard-analysis-service.test.ts` 的旧分块硬切和非空剧本 `[]` 成功预期；增加预算缩批、跨场边界、长单段复合动作、漏末尾却合法的 JSON、分页无重复、对白范围/显式注释匹配、截断与修复耗尽、取消/重启/哈希变化及文件往返测试。人工 89 镜表只作视觉节拍/对白归属对照，不作镜头总数断言。

---

## 四、实施排期

| 阶段 | 任务 | 预计工时 | 依赖 | 状态 |
|------|------|----------|------|------|
| 阶段 1 | 移除 summary 字段 UI | 2h | - | ✅ 已完成 |
| 阶段 1 | 数据迁移逻辑 | 2h | - | ✅ 已完成 |
| 阶段 1 | AI Prompt 调整 | 1h | - | ✅ 已完成 |
| 阶段 2 | 定义标准化选项常量 | 1h | - | ✅ 已完成 |
| 阶段 2 | 更新 UI 选项组件 | 2h | 阶段 2 常量定义 | ✅ 已完成 |
| 阶段 2 | 更新 AI Prompt | 1h | 阶段 2 常量定义 | ✅ 已完成 |
| 阶段 3 | 六类语法解析、原文偏移与视觉事件提取 | 10h | - | ⬜ 未开始 |
| 阶段 3 | 事件组边界、模型预算与分页生成 | 10h | 语法解析 | ⬜ 未开始 |
| 阶段 3 | Prompt/严格响应结构与来源映射 | 6h | 事件组边界 | ⬜ 未开始 |
| 阶段 3 | 对白归属、源覆盖审计与定向修复 | 12h | 来源映射 | ⬜ 未开始 |
| 阶段 3 | 隔离预览、发布事务、取消与恢复 | 10h | 覆盖审计 | ⬜ 未开始 |
| 阶段 3 | 长剧本、人工样本与文件兼容回归测试 | 8h | 上述阶段 3 工作 | ⬜ 未开始 |

**总计**：约 65 小时（阶段 1/2 合计 9 小时，阶段 3 合计 56 小时；按真实模型调试结果调整）。优先交付“解析/分批/审计/原子发布”，再加入草稿预览与跨重启恢复；任何中间交付都不得把未覆盖的结果标记为完整。

---

## 五、验收标准

### 阶段 1 验收
- [x] 编辑面板不再显示"画面内容概述"输入框
- [x] 表格"画面内容描述"列正确显示 `scene` + `action`
- [x] 旧项目打开时自动迁移数据，无报错
- [x] AI 生成的镜头不再包含 `summary` 字段

### 阶段 2 验收
- [x] 景别选项 ≥9 种，镜头运动选项 ≥15 种
- [x] 复杂选项悬停显示详细说明
- [x] AI 输出使用扩充后的标准术语

### 阶段 3 验收
- [ ] 镜头数量由视觉节拍决定，不存在固定镜头配额或按标点机械切分
- [ ] ≥50000 字、多场和单场超长复合动作样本在模型预算内分批处理；每个批次可分页，结果被截断或输出窗口不足时不会发布部分分镜
- [ ] 非空可拍摄输入返回 `[]`、合法 JSON 遗漏末尾事件、非法条目被跳过、错误/重复源 ID 时均不能标记成功；可定向修复，修复耗尽则明确失败
- [ ] 每个必需视觉事件、对白原文片段、字幕与转场都可反查原始偏移；无跨场镜头、无未解释的缺口、重复或越界
- [ ] 分块处理时 UI 在独立草稿预览中显示已验收镜头、源单元覆盖、待确认及待处理进度，正式分镜保持不变
- [ ] 取消、失败和重启不会污染正式文档或 dirty 状态；匹配剧本哈希和规则版本时可续传已审计批次，不匹配时拒绝沿用旧检查点
- [ ] 只有全部批次与全局覆盖校验通过才能原子发布、重排镜头号并显示 100%；提交前保留旧版本快照
- [ ] 场次、动作、角色对白、括号提示、转场/字幕和 HTML 注释六类语法均被正确识别
- [ ] 场次标题形成硬边界，动作与紧随其后的对白不会被分到不同处理块
- [ ] 括号或 HTML 注释中的画面匹配要求被正确执行并绑定对白片段
- [ ] HTML 注释仅进入约束、notes 或 additionalDescription，不被输出为成片画面或对白
- [ ] 未识别或格式异常的内容原样保留在 notes 中并标注[待确认]
- [ ] 对白按画面语义正确绑定，无独立台词镜头、原文改写或对白遗漏
- [ ] Skill 质量校验器与确定性的源覆盖审计通过，阻断性歧义不得伪称完整
- [ ] 原剧本的动作、对白、字幕、转场、特效和人物反应均可追溯且完整覆盖
- [ ] 参考样本仅用于校准画面与对白拆分粒度，景别、机位和运镜不影响拆分边界
- [ ] 《东方交换生日记》第十四集回归样本覆盖人工表体现的主要视觉节拍，镜头数量允许合理浮动
- [ ] 更新旧测试中“按空行硬切”与“非空输入空数组成功”的断言，并通过语法解析、文件往返、长剧本和取消/恢复回归

---

## 六、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 旧数据兼容性问题 | 中 | 高 | 充分测试数据迁移逻辑，保留 summary 字段 |
| AI 输出格式合法但漏镜、漏对白 | 高 | 高 | 每镜源 ID、对白范围的确定性覆盖审计；定向修复耗尽则不发布 |
| 模型输出窗口小于单组所需预算 | 中 | 高 | 小批/分页与实际预算估计；过大组按事件边界拆解，无法拆解时提示更换模型或人工确认 |
| 分块过小导致上下文丢失或跨块重复 | 中 | 中 | 同场只读邻接上下文、显式生成范围、跨批边界复审与去重 |
| 长任务花费高且中断频繁 | 中 | 中 | 按已校验源单元保存轻量检查点、有限定向重试、恢复时校验源哈希和规则版本 |
| 草稿误覆盖正式分镜或恢复时使用过期剧本 | 中 | 高 | 草稿隔离、任务身份及哈希校验、原子发布和发布前版本快照 |
| 语义边界或创作注释有歧义 | 中 | 高 | 保留原文与 `[待确认]` 诊断；阻断性遗漏不自动标记完成，人工样本只用于内容校准 |

---

## 七、后续优化方向

1. **镜头模板库**：为常见场景（对话、追逐、打斗等）提供预设镜头组合
2. **可视化时间轴**：以时间轴形式展示镜头顺序和时长
3. **AI 辅助补全**：用户输入场景后，AI 推荐合适的景别和运镜
4. **多语言支持**：镜头术语支持中英文对照
